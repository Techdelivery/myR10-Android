# Local planning artifacts (plans/specs stay local, never committed)
.omp/

# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android / IDE / local config
local.properties
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/
*.keystore
!debug.keystore

# hardware capture scratch (golden copy lives in protocol/src/test/resources/golden/)
*.hex
*.hex.bak
r10diag.log
# ...but the committed golden capture MUST be tracked
!protocol/src/test/resources/golden/*.hex
